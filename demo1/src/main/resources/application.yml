spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: admin
    url: jdbc:postgresql://localhost:5433/dbvisor
    username: postgres
  jpa:
    database: POSTGRESQL
    hibernate: 
      ddl-auto: update
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
server:
  port: 9090